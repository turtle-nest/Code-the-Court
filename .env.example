# Compose-level env (copy to .env and adjust)
POSTGRES_DB=code_the_court
POSTGRES_USER=nicolas
POSTGRES_PASSWORD=postgres

# Build-time override (optional)
API_DOCKERFILE=Dockerfile
WEB_DOCKERFILE=Dockerfile

# Production only (web_prod will embed this at build time)
PUBLIC_API_URL=/api
